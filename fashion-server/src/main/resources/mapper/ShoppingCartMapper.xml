<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lzl.mapper.ShoppingCartMapper">

    <!--批量加入购物车-->
    <insert id="saveBatch">
        insert into shopping_cart (name, image, user_id, dish_id, setmeal_id, dish_flavor, number, amount, create_time)
        values
        <foreach collection="shoppingCartList" item="sc" separator=",">
            (#{sc.name},#{sc.image},#{sc.userId},#{sc.dishId},#{sc.setmealId},#{sc.dishFlavor},#{sc.number},#{sc.amount},#{sc.createTime})
        </foreach>
        ;
    </insert>


    <!--动态查询数据-->
    <select id="list" resultType="com.lzl.entity.ShoppingCart">
        select *
        from shopping_cart s
        <where>
            <if test="userId != null">
                s.User_id = #{userId}
            </if>
            <if test="dishId != null">
                and s.dish_id = #{dishId}
            </if>
            <if test="dishFlavor != null and dishFlavor != ''">
                and s.dish_flavor = #{dishFlavor}
            </if>
            <if test="setmealId != null">
                and s.setmeal_id = #{setmealId}
            </if>
        </where>
        ;
    </select>
</mapper>
